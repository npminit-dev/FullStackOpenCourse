import { useEffect } from "react";
import { BlogProps } from "../types/types";

const Blogs = ({ token }: BlogProps) => {

  useEffect(() => {
    
  }, [])

  return ( 
    <section>

    </section>
  );
}
 
export default Blogs;